packages: ./literatex.cabal

-- LiterateX works fine with GHC 8.2.2, `text-2`, and `vector-0.13`, but
-- Haddock documentation fails to build.  This constraint allows documentation
-- to build in this case.

constraints:
    text < 2
  , vector < 0.13
